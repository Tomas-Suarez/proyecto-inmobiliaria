using proyecto_inmobiliaria.Enum;
namespace proyecto_inmobiliaria.Constants
{
    public static class Roles
    {
        public const string Administrador = nameof(ERol.Administrador);
        public const string Empleado = nameof(ERol.Empleado);
    }
}